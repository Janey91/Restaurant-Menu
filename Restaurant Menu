using System;

namespace RestaurantMenu
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Cafe Menu");
            Console.Readline();
        }
    }
}

/*
    have a menu that displays 3 options/user picks choice: 
        Updated as of {{DATE last Updated}}
        1. Appetizer
        2. Main Course
        3. Dessert
        4. Add Menu Items
        5. Remove Menu Items
        Console.Readline();

if user picks 1
    1. Appetizer
       Display menu name / Appetizer - Console.WriteLine("Appetizer" / [1])
        Console.WriteLine(*****)
       foreach menu item in appetizer)
        if(new)
            {
                Console.Write(**new**)
            }
        ORR if(new)
            append(*NEW*) to [0] after below is done)

            Console.WriteLine({Display menu item} - {price:C} 
            Console.WriteLine{description}
        
    (   **repeat
    if user picks 2 
        2. Main Course

    if user picks 3
        3. Dessert )

if user picks 4
    Add an item to: (Cycle through menu choices)   //needs - name, price, description & category
        1. Appetizer: Crab Rangoon Wonton Cups- $9.00
        2. Main Course: Grilled Coconut Shrimp With Shishito Peppers $22.00
        3. Dessert: Chocolate hazelnut ice cream cheesecake $10.99

    user picks a menu- Console.ReadLine();
        (userinput chooses the menu and holds item for below)
    List newItem;
        Console.WriteLine(Add the {{menu}} Crab Rangoon Wonton Cups:)

        Console.WriteLine(Chocolate hazelnut ice cream cheesecake:);
        newitem = Console.ReadLine();

        Console.WriteLine(Add the {{item/newItem[0]}} 10.99:);
        newitem +=Console.ReadLine();

        Console.WriteLine(Add the {{Chocolate hazelnut ice cream cheesecake/Crab Rangoon Wonton Cups[0]}} Grilled Coconut Shrimp With Shishito Peppers);
        newitem +=Console.ReadLine();

        SelectedMenu.ADD(newitem);

        Console.WriteLine(added new item);

if user picks 5
        Console.WriteLine( please remove from the item from the {{menu}}?
        removeItem = Console.ReadLine()

        if MenuList.Contains(removeItem) or have user select a menu and then ask them for the item.
        
        else{Item is not on the menu! TRY another option.}
        */
